const BASE_URL = "https://7c5c-223-235-179-251.ngrok.io";

// Paths
const OTP = `${BASE_URL}/auth/otp`;
const SIGNUP = `${BASE_URL}/auth/signup`;
const LOGIN = `${BASE_URL}/auth/login`;
const LOGOUT = `${BASE_URL}/auth/logout`;
const CHECK_LOGIN = `${BASE_URL}/auth/check`;

export {BASE_URL, OTP, SIGNUP, LOGIN, LOGOUT, CHECK_LOGIN};
