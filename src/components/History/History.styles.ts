import { StyleSheet } from "react-native";

const styles = StyleSheet.create({
	shimmer: {
		borderRadius: 20,
		marginBottom: 20,
		minHeight: 100,
		width: "100%",
	},
	wrapper: {
		marginTop: 10,
		minHeight: 120,
		width: "100%",
	}
});

export default styles;
